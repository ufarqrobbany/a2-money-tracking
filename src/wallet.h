#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Wallet {
    int id;
    char nama_dompet[20];
    int saldo;
};

void buatDompetAwal(char username[20]);

// getSaldo
// kurangiSaldo
// tambahSaldo
// tambahDompet